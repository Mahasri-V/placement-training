import numpy as np

# Generate random numbers from a uniform distribution
rand_array = np.random.rand(3, 3)
print("Random Array:\n", rand_array)

# Generate random integers between 0 and 10
rand_int_array = np.random.randint(0, 10, (2, 3))
print("Random Integer Array:\n", rand_int_array
